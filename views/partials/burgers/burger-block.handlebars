<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newburger="{{devour}}">
		{{#if order}}Order{{else}}Devour!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>